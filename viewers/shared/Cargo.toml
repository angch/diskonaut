[package]
name = "duscape-viewer"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
homepage.workspace = true
description = "What the duscape desktop viewers share: the window's state and layout, the first scan with its live outline, and the preview reader — with no toolkit in it."

[lints]
workspace = true

[dependencies]
libduscape = { workspace = true }
duscape-scan = { workspace = true }

//! The `diskonaut` alias: the same program as `diskonaut-angch`, under the upstream command name,
//! so existing docs, muscle memory, and benchmark recipes keep working.
fn main() {
    diskonaut_angch::run();
}
